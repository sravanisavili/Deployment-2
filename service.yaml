apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: mysql-deployment
  ports:
    - port: 80
      
      targetPort: 80
      
      nodePort: 30007